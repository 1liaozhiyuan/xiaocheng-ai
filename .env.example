# 复制本文件为 .env 并填入真实值

# 智谱开放平台（对话 + 记忆抽取）https://open.bigmodel.cn 注册后创建 API Key
ZHIPU_API_KEY=你的key

# 阿里云百炼（向量化检索，可选）https://bailian.console.aliyun.com
QWEN_API_KEY=你的key

# ── 以下均为可选，不填使用代码默认值 ──

# 对话模型（默认 glm-4.5-flash，免费）/ 抽取模型（默认 glm-4-flash）
# CHAT_MODEL=glm-4.5-flash
# LITE_MODEL=glm-4-flash

# 向量化模型（默认 qwen3.7-text-embedding-flash，走 QWEN_API_KEY）
# 换其他 OpenAI 兼容厂商时同步设置 EMBED_BASE_URL
# EMBED_MODEL=qwen3.7-text-embedding-flash
# EMBED_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# GLM-4.5 系关闭思考模式（默认 true）；换其他厂商报参数错误时设为 false
# DISABLE_THINKING=true

# 人设定制
# PERSONA_NAME=小澄

# Web 服务端口（默认 8001）
# SERVER_PORT=8001

# 危机 L3 复核模型（安全关键，可选付费强模型提升隐晦意念召回）
# CRISIS_RECHECK_MODEL=glm-4.5-plus
